{
  "name": "jedi",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "check:boundaries": "node internal/check-boundaries.mjs",
    "dev:examples": "pnpm -F @jedi/examples dev",
    "dev:docs": "pnpm -F @jedi/docs-app dev",
    "dev:playground": "pnpm -F @jedi/playground dev",
    "metrics:bundle": "node internal/metrics/bundle-size.mjs",
    "metrics:tokens": "node internal/metrics/token-coverage.mjs",
    "metrics:api": "node internal/metrics/api-snapshot.mjs",
    "metrics": "pnpm metrics:bundle && pnpm metrics:tokens && pnpm metrics:api",
    "check:app-boundaries": "node internal/check-app-boundaries.mjs",
    "verify": "pnpm test && pnpm build && pnpm check:boundaries && pnpm check:app-boundaries && pnpm metrics"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
